---
const { classname, draw, bg } = Astro.props;
---

<div class={`absolute ${classname} z-0`}>
  <div
    class={`block w-full h-full bg-no-repeat ${bg}`}
    style=`background-image: url('/draws/${draw}.svg');`
  >
  </div>
</div>
